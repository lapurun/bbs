<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <div class="wrapper">
      <div class="main article">
          <h1 class="title"><a href="/"><%= title %></a></h1>
        <p>
          <%= title %>とはTeX表記の使える理系のための掲示板です。このようなサイトに需要があるかさておき、似たようなサイトとして
          <a href="https://physics.stackexchange.com/">stackexchange.com</a>
          や<a href="https://chiebukuro.yahoo.co.jp/">yahoo知恵袋</a>などがありますが、
          このサイトはその中間を目指しています。
          完成度は残念です。未完成なのでいろいろバージョンアップしていきます。
        </p>
        <div>
          <ul class="navbar">
            <li><a href="/admin">管理人だより</a></li>
            <li><a href="/tex">TeXについて</a></li>
            <li><a href="/practice">TeX練習機</a></li>
          </ul>
        </div>
        
        <hr>
        <h2>スレッド一覧</h2>
        <table>
          <tr>
            <th align="left">id</th>
            <th align="left">title</th>
            <th align="left">creation date</th>
          </tr>
          <% for(var i in content){ %>
            <tr>
              <% var obj = content[i]; %>
              <td colspan="3"><a href="thread?gn=<%= obj.id %>"><%= obj.id %> <%= obj.title %> <%= obj.date %></a></td>
            </tr>
          <% } %>
      </table>
      <hr>
      <h2>スレッド作成</h2>
        <form method="POST" action="/">
        <table>
          <tr>
            <th align="left">タイトル</th>
            <td><input type="text" name="title" size="50" required></td>
          </tr>
          <tr>
            <th align="left">カテゴリー</th>カテゴリー:未完成
              <td>
                <select name="category" required>
                <option value="" selected>選択してください</option>
                <% for(var i in category){ %>
                <% var obj = category[i]; %>
                <option value=<%= obj.category %>><%= obj.category %></option>
                <% } %>
                </select>
              </td>
          </tr>
        </table>
        <input type="submit" value="作成" size="50">
        </form>
      </div>
      <div class="main ad">
        <p>広告:</p>
      </div>
    </div>
  </body>
</html>
